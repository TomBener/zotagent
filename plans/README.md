# Implementation Plans

Generated by the improve skill on 2026-07-03 (audit at commit `9b80ba6`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

The session ran non-interactively, so per the skill's default the top 5
findings by leverage were planned. The remaining vetted findings are recorded
below as candidates for a future planning pass.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Run the full test suite in CI on every push and PR | P1 | S | — | DONE |
| 002  | Fix stale `excludes.txt` documentation (AGENTS.md + SKILL.md) | P1 | S | — | DONE |
| 003  | Atomic catalog.json write + corrupt-file-tolerant read | P1 | S | 001 | DONE |
| 004  | Owner-only permissions on `~/.zotagent/config.json` | P1 | S | 001 | DONE |
| 005  | Keyword indexing skips unreadable manifests | P2 | S | 001 | DONE |
| 006  | Reject attachment paths escaping `attachmentsRoot` via dot segments | P1 | S | — | DONE |
| 007  | Resolve transitive npm audit advisories under `@tobilu/qmd` | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 003/004/005 list 001 as a soft dependency: they are independently
  implementable, but landing 001 first means their test additions are enforced
  on PRs rather than only locally. If executing in parallel, that ordering is
  a nice-to-have, not a hard block.
- 002 is fully independent (docs only).

## Vetted findings not yet planned (candidates for a next pass)

Ordered by leverage. Each was confirmed against the code at `9b80ba6`.

- **CORRECTNESS-02** — Zotero item creation is non-idempotent: `createWriteToken()`
  (`src/add.ts:125`) returns a fresh token per call, so a post-commit timeout
  (8s, `src/add.ts:42`) followed by a retry duplicates items. Effort M.
- **PERF-01** — `add --json` batch is serial and re-fetches the identical item
  template per item (`src/add.ts:1284,1292`); Zotero accepts 50 items per POST.
  Effort M. Interacts with CORRECTNESS-02 — plan them together.
- **DEBT-02** — Zotero HTTP plumbing duplicated across modules:
  `fetchWithTimeout` ×3 (`add.ts:666`, `s2.ts:100`, `zotero-read.ts:31`),
  `readJsonResponse` ×2, timeout constants diverged (8s vs 30s). Effort M.
  Natural prerequisite for the CORRECTNESS-02/PERF-01 work.
- **SECURITY-01** — planned as 006.
- **CORRECTNESS-04** — exclude-tag fetch fails open: on a network/auth error
  `fetchTaggedItemKeys` (`src/sync.ts:802-828`) returns an empty set with only
  a log warning, so excluded items get indexed until the next successful sync.
  Partially acknowledged in the code comment at `sync.ts:795-801`; the fix is
  to abort or reuse the previous exclusion set on genuine fetch errors. Effort S.
- **DEPS-01/DEBT-03** — `@types/better-sqlite3` sits in `dependencies`
  (package.json:28); move to `devDependencies`. Trivial.
- **SECURITY-03** — planned as 007.
- **PERF-02** — `metadata` re-normalizes every record field per query
  (`src/metadata.ts:127-147` → `normalizeExactText`, `src/exact.ts:69-76`);
  precompute normalized fields in `loadCatalog`. Effort M.
- **DEBT-04** — `escapeRegExp` duplicated (`engine.ts:231`, `exact.ts:102`);
  CJK script-range regexes hand-written in ~7 places. Effort S.
- **DEBT-05** — manifest gzip I/O lives in the general `utils.ts` grab-bag;
  `scripts/compare-formats.ts` is unreferenced. Effort S.
- **TEST-02** — no integration coverage for `status`/`recent` CLI wiring
  (`tests/integration/cli.test.ts` never invokes them). Effort S.
- **DX-01** — no linter beyond `tsc`, no formatter, no pre-commit hook.
  Effort S-M; maintainer taste decision.
- **DX-02** — `npm run check` ≈ 37s, dominated by per-test subprocess spawns in
  the integration suite; split `test:unit` for the inner loop. Effort M.
- **DEBT-01** — `src/sync.ts` is a 2239-line god module (~8 responsibilities,
  only `runSync` consumed by production code). Highest-impact refactor but
  multi-day; do after the smaller sync-adjacent fixes land. Effort L.
- **SECURITY-04** (investigate) — document text is emitted to agents with no
  data/instruction framing (`src/json.ts:1-14`); prompt-injection surface
  inherent to retrieval tools. Low confidence; design question, not a bug.
- **DEPS-02** (note) — `@tobilu/qmd` is a young single-maintainer package on
  the semantic-search critical path; `src/qmd.ts` already isolates it — keep
  that boundary clean.
- **DEPS-03** (note) — TypeScript 6.0 available; adopt deliberately on a branch.

## Direction findings (maintainer options, not ranked against bugs)

- **DIRECTION-01** — `add` exists but no `update`/`delete` (zero PATCH/DELETE
  in `src/`); agents can create items but not fix or remove them. Design/spike
  plan with version-guarded writes (`If-Unmodified-Since-Version`) and
  explicit destructive-op guardrails. Effort L (coarse).
- **DIRECTION-02** — MCP server wrapper: every command already emits a stable
  JSON envelope (`src/json.ts` `emitOk`/`emitError`), making tool exposure
  disproportionately cheap. Effort M (coarse).
- **DIRECTION-03** — `sync --watch` / auto-index-after-add: SKILL.md:210
  documents the "not searchable until next sync" friction, and the incremental
  index machinery already exists and is tested. Effort M (coarse).

## Findings considered and rejected

- Per-collection fetch loop (`zotero-collections.ts:32`) and pagination loop
  (`recent.ts:126`) as N+1: required by the Zotero API shape; no batch endpoint.
- `excludes.ts` / `heuristics.ts` as dead code: both are live
  (`sync.ts:21`/`:1357`; `engine.ts:8`, `manifest.ts:4`).
- opencc re-instantiation per call: already memoized (`zh-convert.ts:45-52`).
- Bibliography re-parse / manifest re-read in hot paths: already cached
  (`engine.ts:75-87`).
- Missing SQLite transactions/prepared statements in bulk inserts: already
  present (`keyword-db.ts:374-384`).
- `sync.test.ts` "mocks away the logic": false — it drives the real `runSync`
  against real temp filesystems with injected extractors.
- Honoring `translationServerUrl` / fetching user-supplied URLs: by design for
  a local CLI.
- `opencc-js` / `@tobilu/qmd` as abandoned deps: both actively released
  (July 2026 / May 2026).
